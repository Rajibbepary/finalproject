import SectionTitle from "../../../components/SectionTitle/SectionTitle";


const Payment = () => {
    return (
        <div>
            <SectionTitle heading={'payment'} subHeading={'Pleace Payment'}></SectionTitle>
        </div>
    );
};

export default Payment;